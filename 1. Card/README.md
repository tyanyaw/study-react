Creating Card with pure html code
then add React and ReactDOOM

then add component "Card", 
wrap it in "App" component,
in APP we input data, in CARD we got placeholder to place data

then render at ReactDOOM
